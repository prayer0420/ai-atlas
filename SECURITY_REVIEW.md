# AI Atlas 보안 점검 — 2026-09-21

## 원인과 재발 방지 — 2026-09-22

이번에 확인한 것은 취약한 설계와 구현이며, 실제 침해가 발생했다는 판정은 아니다.

| 문제 | 코드에서 확인한 원인 | 방지 장치 |
| --- | --- | --- |
| 초기 비밀번호 선점 가능성 | 아이디와 초기 설정 여부를 확인했지만 요청자의 소유권을 확인하기 전에 관리자 권한을 사용했다. | 세션·허용 계정 검증을 먼저 실행하고 세션 소유자만 변경. 무인증 요청 거부 회귀 테스트. |
| 다른 계정의 API 접근 경계 불일치 | 유효한 Supabase 로그인과 개인 자료실 사용 권한을 모든 API에서 일관되게 구분하지 못했다. | 공통 인증 함수의 허용 목록 검사와 다른 계정 거부 테스트. |
| 자식 프로세스의 과도한 권한 정보 | 외부 프로그램 실행 시 서버 환경 전체를 상속했다. | 필요한 OS 환경변수만 허용하고 비밀키·런타임 주입 변수 미전달 테스트. |
| 원문 노출 가능성 | 원문을 명령 인자에 넣고 외부 오류가 원문을 포함할 가능성을 충분히 제한하지 않았다. | 표준입력 전달과 오류 로그 최소화. |
| 요청 크기 제한 우회 | Content-Length만 신뢰하면 헤더가 없는 요청의 실제 크기를 제한하지 못한다. | 읽는 도중 바이트 수 제한 및 헤더 없는 초과 요청 테스트. |

개발 과정의 원인 분석: 로그인 편의와 기능 성공 경로에 비해, 비로그인·다른 사용자·외부 프로세스 같은 신뢰 경계 검토가 부족했다. 이는 코드와 기존 검증 범위에서 도출한 평가이며, 작업 당시의 의도를 입증하는 기록은 아니다. 사용자 요청이 보안을 생략하라는 뜻은 아니며 구현·검증 책임은 개발 과정에 있다.

재발 방지를 위해 AGENTS.md에 위 경계를 필수 작업 규칙으로 추가했다. package.json의 build는 전체 회귀 테스트 성공 후에만 Next.js 빌드를 실행하며, vercel.json도 이 명령을 사용하도록 명시했다. 테스트 실패 시 해당 빌드가 중단된다. 이는 모든 취약점을 탐지하는 보증이나 GitHub 브랜치 보호 설정은 아니며, 새 기능의 새로운 위험에는 별도 테스트와 검토가 필요하다.

## 확인한 문제와 수정

- **초기 비밀번호 설정의 소유권 검증 누락**: 기존 API는 로그인 없이 아이디로 초기 비밀번호를 지정할 수 있었다. 현재 운영 계정은 초기 설정 완료 상태여서 해당 경로가 닫혀 있었지만, 신규 초기 설정 시 계정 선점 위험이 있었다. 요청 첫 단계에서 유효한 로그인과 소유자 허용 목록을 확인하고, 세션 소유자의 계정만 변경하도록 수정했다. 로그인 화면의 무인증 초기 설정 링크도 제거했다. 기존 비밀번호는 변경하지 않았다.
- **프로바이더 프로세스에 과도한 환경변수 전달**: Hermes와 Aside에 서버 환경 전체가 상속되었다. OS 경로·런타임 탐색에 필요한 변수만 전달하도록 변경했다. Supabase 관리 키, Cron 비밀값, 기타 API 키, 런타임 주입 변수는 전달하지 않는다. Hermes의 기존 로그인 정보는 해당 프로그램이 자기 프로필에서 읽는다.
- **원문이 프로세스 인자·오류에 포함될 가능성**: Hermes 입력을 표준입력으로 전달하고 오류 로그에서 원문과 프로바이더 오류 본문을 제거했다. 분석 호출은 설치된 Hermes에서 도구가 없는 선택으로 실행한다. 이는 OS 수준의 프로세스 격리를 의미하지 않는다.
- **개인 자료실 접근 경계**: 모든 인증 API에서 허용 계정을 확인한다. 유효한 다른 Supabase 계정도 이 개인 자료실 API를 사용할 수 없다. 기존 사용자별 RLS도 유지한다.
- **대용량 요청**: Content-Length가 없는 요청도 스트림을 읽는 동안 300KB 한도를 적용한다.
- **검증용 화면**: 실제 결과를 확인하기 위해 잠시 만든 로컬 검증 페이지는 배포 코드에서 제거했다.

## 검증 범위와 결과

- 무인증 초기 비밀번호 요청 401, 다른 계정의 API 접근 403 회귀 테스트.
- 자식 프로세스 환경의 비밀값 및 NODE_OPTIONS/PYTHONPATH 미전달 테스트.
- 요청 크기 제한, SSRF 방어, 사용자별 RLS·작업 큐 격리, 카드 근거 등 전체 자동 테스트 48개 통과.
- 운영 의존성 `npm audit --omit=dev`: 알려진 취약점 0건. 점검 시점 기준이며 안전성의 절대 보증은 아니다.
- Git 이력의 관련 텍스트 파일 버전 225개를 현재 환경의 비밀값과 대조: 일치 없음. 과거에 폐기된 키나 알 수 없는 형태의 비밀값까지 검출했다는 뜻은 아니다.
- 운영 DB의 AI Atlas 테이블 11개 모두 RLS 활성화. 로그인 매핑 테이블은 정책 없이 일반 역할 접근을 거부하고 서버에서만 사용한다.

## 남은 사항

- Supabase 공용 프로젝트의 기존 `set_updated_at` 함수 search_path 경고와 유출 비밀번호 검사 비활성 경고가 남아 있다. 다른 프로젝트와 공유된 설정이므로 이번 앱 수정에서 변경하지 않았다.
- 이번 점검은 코드·접근 경계·현재 DB 설정 점검이다. 과거 침해 유무를 판정하는 포렌식 조사는 수행하지 않았다.
- 외부 로그인 필요 페이지, Notion 동적 본문, 영상·이미지 속 내용은 읽기 제한이 남아 있다. 해당 자료를 읽은 것처럼 분석하지 않는다.
